const defaultState = {

}

function reducer(prevState = defaultState, action) {
    switch(action.type) {
        // case "LOG_IN":
        //     return {...prevState, currentUser: //current user here}
        default:
            return prevState
    }
}

export default reducer